filesToModify:
  - path: ext_emconf.php
    patterns:
      - "'version' => '{%version%}'"
    reportUnmatched: true

releaseOptions:
  commitMessage: '[RELEASE] Release of EXT:cache_bags {%version%}'
